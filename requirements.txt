# Install PyTorch separately using the command for your CUDA/CPU platform from
# https://pytorch.org/get-started/locally/, then install this file.
transformers>=4.50
segmentation-models-pytorch>=0.4  # U-Net baselines with ImageNet-pretrained encoders.
monai>=1.3                         # Optional MONAI FlexibleUNet baselines.
# Keep NumPy 1.x: cluster-provided SciPy/scikit-learn binaries may not yet be
# compatible with NumPy 2.x.
numpy>=1.24,<2
pandas>=2.0
opencv-python>=4.8
Pillow>=10.0
PyYAML>=6.0
wandb>=0.16
